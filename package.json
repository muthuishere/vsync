{
  "name": "@muthuishere/secret-lib",
  "version": "0.1.0",
  "description": "Encrypted env-sync for repos: zip + AES-GCM + S3 + manifest-sealed versioning. Run via bunx.",
  "type": "module",
  "bin": {
    "secret-lib": "./bin/secret-lib.ts"
  },
  "scripts": {
    "test": "bun test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/muthuishere/secret-lib.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
