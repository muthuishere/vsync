{
  "name": "@muthuishere/vsync",
  "version": "0.9.1",
  "description": "Encrypted secret-sync CLI for small teams. AES-256-GCM vault on any S3-compatible bucket + OS keychain key + one-passphrase .share onboarding + fanout to GitHub Actions, GCP Secret Manager, AWS Secrets Manager, Azure Key Vault, HashiCorp Vault KV v2 + append-only audit log. Worktree-safe. Bun-native, run via bunx.",
  "type": "module",
  "bin": {
    "vsync": "./bin/vsync.ts"
  },
  "files": [
    "bin",
    "src",
    "skills",
    "README.md"
  ],
  "scripts": {
    "test": "bun test",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "keywords": [
    "secrets",
    "env",
    "dotenv",
    "vault",
    "s3",
    "keychain",
    "encryption",
    "bun",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/muthuishere/vsync.git"
  },
  "homepage": "https://github.com/muthuishere/vsync#readme",
  "bugs": {
    "url": "https://github.com/muthuishere/vsync/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "bun": ">=1.2.21"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "vitepress": "^1.6.4"
  }
}
