node_modules/
*.log

# VitePress build output + cache (docs site)
docs/.vitepress/dist/
docs/.vitepress/cache/

# `vsync use` creates a symlink here; never commit it.
.env

# Real env-config JSON files contain plaintext secrets — never committed.
# Only the placeholder sample is committed.
envconfig.*.json
!envconfig.sample.json
infra/vault/

# macOS Finder metadata — never useful in the repo.
.DS_Store
